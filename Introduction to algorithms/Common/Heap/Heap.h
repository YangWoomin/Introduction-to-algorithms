#pragma once

#include <vector>

template <typename Key, typename Element>
class Heap
{
public:
	typedef std::pair<Key, Element> Element;

protected:
	std::vector<Element> _elements;

public:
	Heap(std::size_t n)
	{
		
	}

	

};
